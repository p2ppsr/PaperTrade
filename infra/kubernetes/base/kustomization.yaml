apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - namespace.yaml
  - configmap.yaml
  - service.yaml
  - workload.yaml
  - pdb.yaml

commonLabels:
  app.kubernetes.io/part-of: papertrade
