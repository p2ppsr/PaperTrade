apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - namespace.yaml
  - configmap.yaml
  - pvc.yaml
  - service.yaml
  - workload.yaml
  - ingress.yaml

commonLabels:
  app.kubernetes.io/part-of: papertrade
