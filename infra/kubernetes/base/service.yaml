apiVersion: v1
kind: Service
metadata:
  name: papertrade
  labels:
    app.kubernetes.io/name: papertrade
    app.kubernetes.io/component: api
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: papertrade
    app.kubernetes.io/component: api
  ports:
    - name: http
      port: 8080
      targetPort: http
