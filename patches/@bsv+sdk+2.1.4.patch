diff --git a/node_modules/@bsv/sdk/dist/cjs/src/auth/Peer.js b/node_modules/@bsv/sdk/dist/cjs/src/auth/Peer.js
index 9a0265b..2a4f93f 100644
--- a/node_modules/@bsv/sdk/dist/cjs/src/auth/Peer.js
+++ b/node_modules/@bsv/sdk/dist/cjs/src/auth/Peer.js
@@ -298,8 +298,9 @@ class Peer {
             initialNonce: sessionNonce,
             requestedCertificates: this.certificatesToRequest
         };
+        const initialResponse = this.waitForInitialResponse(sessionNonce);
         await this.transport.send(initialRequest);
-        return await this.waitForInitialResponse(sessionNonce);
+        return await initialResponse;
     }
     /**
      * Waits for the initial response from the peer after sending an initial handshake request message.
@@ -663,15 +664,16 @@ class Peer {
             throw new Error(`Unable to verify nonce for general message from: ${message.identityKey}`);
         }
         const peerSession = await this.sessionManager.getSession(message.yourNonce);
-        if (peerSession == null) {
+        const recoveredStatelessSession = peerSession == null;
+        if (peerSession == null && typeof message.identityKey !== 'string') {
             throw new Error(`Session not found for nonce: ${message.yourNonce}`);
         }
-        const certificatesRequired = peerSession.certificatesRequired === true;
-        const certificatesValidated = peerSession.certificatesValidated === true;
+        const certificatesRequired = peerSession?.certificatesRequired === true;
+        const certificatesValidated = peerSession?.certificatesValidated === true;
         // If certificates are required but not yet validated, wait for them with a timeout
         if (certificatesRequired && !certificatesValidated) {
             const CERTIFICATE_WAIT_TIMEOUT_MS = 30000;
-            const sessionNonce = peerSession.sessionNonce;
+            const sessionNonce = peerSession?.sessionNonce;
             if (sessionNonce == null) {
                 throw new Error('Session nonce is required for certificate validation');
             }
@@ -681,7 +683,7 @@ class Peer {
                     const promise = this.certificateValidationPromises.get(sessionNonce);
                     if (promise != null) {
                         this.certificateValidationPromises.delete(sessionNonce);
-                        reject(new Error(`Timeout waiting for certificate validation from peer ${peerSession.peerIdentityKey ?? 'unknown'}`));
+                        reject(new Error(`Timeout waiting for certificate validation from peer ${peerSession?.peerIdentityKey ?? 'unknown'}`));
                     }
                 }, CERTIFICATE_WAIT_TIMEOUT_MS);
                 // Ensure the timer doesn't prevent process exit during tests
@@ -701,19 +703,23 @@ class Peer {
                 });
             });
         }
+        const sessionNonceForSignature = peerSession?.sessionNonce ?? message.yourNonce;
+        const counterpartyForSignature = peerSession?.peerIdentityKey ?? message.identityKey;
         const { valid } = await this.wallet.verifySignature({
             data: message.payload,
             signature: message.signature,
             protocolID: [2, 'auth message signature'],
-            keyID: `${message.nonce ?? ''} ${peerSession.sessionNonce ?? ''}`,
-            counterparty: peerSession.peerIdentityKey
+            keyID: `${message.nonce ?? ''} ${sessionNonceForSignature}`,
+            counterparty: counterpartyForSignature
         }, this.originator);
         if (!valid) {
-            throw new Error(`Invalid signature in generalMessage from ${peerSession.peerIdentityKey}`);
+            throw new Error(`Invalid signature in generalMessage from ${counterpartyForSignature}`);
+        }
+        if (!recoveredStatelessSession && peerSession != null) {
+            // Mark last usage
+            peerSession.lastUpdate = Date.now();
+            await this.sessionManager.updateSession(peerSession);
         }
-        // Mark last usage
-        peerSession.lastUpdate = Date.now();
-        await this.sessionManager.updateSession(peerSession);
         // Update lastInteractedWithPeer
         this.lastInteractedWithPeer = message.identityKey;
         // Dispatch callbacks
diff --git a/node_modules/@bsv/sdk/dist/cjs/src/auth/clients/AuthFetch.js b/node_modules/@bsv/sdk/dist/cjs/src/auth/clients/AuthFetch.js
index 105941f..27664a1 100644
--- a/node_modules/@bsv/sdk/dist/cjs/src/auth/clients/AuthFetch.js
+++ b/node_modules/@bsv/sdk/dist/cjs/src/auth/clients/AuthFetch.js
@@ -223,6 +223,7 @@ class AuthFetch {
                     });
                 }
                 // Send the request, now that all listeners are set up
+                let handledRetry = false;
                 await peerToUse.peer.toPeer(writer.toArray(), peerToUse.identityKey).catch(async (error) => {
                     const isStaleSession = error.message.includes('Session not found for nonce') ||
                         (error.message.includes('without valid BSV authentication') &&
@@ -235,23 +236,35 @@ class AuthFetch {
                         delete this.peers[baseURL];
                         config.retryCounter ?? (config.retryCounter = 3);
                         const response = await this.fetch(url, config);
+                        handledRetry = true;
                         resolve(response);
                         return;
                     }
                     if (error.message.includes('HTTP server failed to authenticate')) {
                         try {
                             const response = await this.handleFetchAndValidate(url, config, peerToUse);
+                            handledRetry = true;
                             resolve(response);
                             return;
                         }
                         catch (fetchError) {
+                            handledRetry = true;
                             reject(fetchError);
                         }
                     }
                     else {
+                        handledRetry = true;
                         reject(error);
                     }
                 });
+                if (!handledRetry && this.callbacks[requestNonceAsBase64] != null) {
+                    peerToUse.peer.stopListeningForGeneralMessages(listenerId);
+                    delete this.callbacks[requestNonceAsBase64];
+                    delete this.peers[baseURL];
+                    config.retryCounter ?? (config.retryCounter = 3);
+                    const response = await this.fetch(url, config);
+                    resolve(response);
+                }
             }
             catch (e) {
                 reject(e);
diff --git a/node_modules/@bsv/sdk/dist/cjs/src/auth/transports/SimplifiedFetchTransport.js b/node_modules/@bsv/sdk/dist/cjs/src/auth/transports/SimplifiedFetchTransport.js
index 4d5f3e2..01920dc 100644
--- a/node_modules/@bsv/sdk/dist/cjs/src/auth/transports/SimplifiedFetchTransport.js
+++ b/node_modules/@bsv/sdk/dist/cjs/src/auth/transports/SimplifiedFetchTransport.js
@@ -101,7 +101,7 @@ class SimplifiedFetchTransport {
                         }
                         if (this.onDataCallback != null) {
                             const responseMessage = await response.json();
-                            this.onDataCallback(responseMessage);
+                            await this.onDataCallback(responseMessage);
                         }
                         if (message.messageType === 'initialRequest') {
                             resolve();
@@ -239,7 +239,7 @@ class SimplifiedFetchTransport {
                 throw this.createUnauthenticatedResponseError(url, response, responseBodyArray);
             }
             // Handle the response if data is received and callback is set
-            this.onDataCallback(responseMessage);
+            await this.onDataCallback(responseMessage);
         }
     }
     /**
@@ -250,11 +250,8 @@ class SimplifiedFetchTransport {
      * @returns A promise that resolves once the callback is set.
      */
     async onData(callback) {
-        this.onDataCallback = (m) => {
-            void callback(m).catch(() => {
-                // Errors from handleIncomingMessage on the client side are not
-                // actionable here — prevent unhandled promise rejections.
-            });
+        this.onDataCallback = async (m) => {
+            await callback(m);
         };
     }
     createNetworkError(url, originalError) {
diff --git a/node_modules/@bsv/sdk/dist/esm/src/auth/Peer.js b/node_modules/@bsv/sdk/dist/esm/src/auth/Peer.js
index ed1545f..ef56746 100644
--- a/node_modules/@bsv/sdk/dist/esm/src/auth/Peer.js
+++ b/node_modules/@bsv/sdk/dist/esm/src/auth/Peer.js
@@ -291,8 +291,9 @@ export class Peer {
             initialNonce: sessionNonce,
             requestedCertificates: this.certificatesToRequest
         };
+        const initialResponse = this.waitForInitialResponse(sessionNonce);
         await this.transport.send(initialRequest);
-        return await this.waitForInitialResponse(sessionNonce);
+        return await initialResponse;
     }
     /**
      * Waits for the initial response from the peer after sending an initial handshake request message.
@@ -656,15 +657,16 @@ export class Peer {
             throw new Error(`Unable to verify nonce for general message from: ${message.identityKey}`);
         }
         const peerSession = await this.sessionManager.getSession(message.yourNonce);
-        if (peerSession == null) {
+        const recoveredStatelessSession = peerSession == null;
+        if (peerSession == null && typeof message.identityKey !== 'string') {
             throw new Error(`Session not found for nonce: ${message.yourNonce}`);
         }
-        const certificatesRequired = peerSession.certificatesRequired === true;
-        const certificatesValidated = peerSession.certificatesValidated === true;
+        const certificatesRequired = peerSession?.certificatesRequired === true;
+        const certificatesValidated = peerSession?.certificatesValidated === true;
         // If certificates are required but not yet validated, wait for them with a timeout
         if (certificatesRequired && !certificatesValidated) {
             const CERTIFICATE_WAIT_TIMEOUT_MS = 30000;
-            const sessionNonce = peerSession.sessionNonce;
+            const sessionNonce = peerSession?.sessionNonce;
             if (sessionNonce == null) {
                 throw new Error('Session nonce is required for certificate validation');
             }
@@ -674,7 +676,7 @@ export class Peer {
                     const promise = this.certificateValidationPromises.get(sessionNonce);
                     if (promise != null) {
                         this.certificateValidationPromises.delete(sessionNonce);
-                        reject(new Error(`Timeout waiting for certificate validation from peer ${peerSession.peerIdentityKey ?? 'unknown'}`));
+                        reject(new Error(`Timeout waiting for certificate validation from peer ${peerSession?.peerIdentityKey ?? 'unknown'}`));
                     }
                 }, CERTIFICATE_WAIT_TIMEOUT_MS);
                 // Ensure the timer doesn't prevent process exit during tests
@@ -694,19 +696,23 @@ export class Peer {
                 });
             });
         }
+        const sessionNonceForSignature = peerSession?.sessionNonce ?? message.yourNonce;
+        const counterpartyForSignature = peerSession?.peerIdentityKey ?? message.identityKey;
         const { valid } = await this.wallet.verifySignature({
             data: message.payload,
             signature: message.signature,
             protocolID: [2, 'auth message signature'],
-            keyID: `${message.nonce ?? ''} ${peerSession.sessionNonce ?? ''}`,
-            counterparty: peerSession.peerIdentityKey
+            keyID: `${message.nonce ?? ''} ${sessionNonceForSignature}`,
+            counterparty: counterpartyForSignature
         }, this.originator);
         if (!valid) {
-            throw new Error(`Invalid signature in generalMessage from ${peerSession.peerIdentityKey}`);
+            throw new Error(`Invalid signature in generalMessage from ${counterpartyForSignature}`);
+        }
+        if (!recoveredStatelessSession && peerSession != null) {
+            // Mark last usage
+            peerSession.lastUpdate = Date.now();
+            await this.sessionManager.updateSession(peerSession);
         }
-        // Mark last usage
-        peerSession.lastUpdate = Date.now();
-        await this.sessionManager.updateSession(peerSession);
         // Update lastInteractedWithPeer
         this.lastInteractedWithPeer = message.identityKey;
         // Dispatch callbacks
diff --git a/node_modules/@bsv/sdk/dist/esm/src/auth/clients/AuthFetch.js b/node_modules/@bsv/sdk/dist/esm/src/auth/clients/AuthFetch.js
index 7838555..7ebccb0 100644
--- a/node_modules/@bsv/sdk/dist/esm/src/auth/clients/AuthFetch.js
+++ b/node_modules/@bsv/sdk/dist/esm/src/auth/clients/AuthFetch.js
@@ -188,6 +188,7 @@ export class AuthFetch {
                     });
                 }
                 // Send the request, now that all listeners are set up
+                let handledRetry = false;
                 await peerToUse.peer.toPeer(writer.toArray(), peerToUse.identityKey).catch(async (error) => {
                     const isStaleSession = error.message.includes('Session not found for nonce') ||
                         (error.message.includes('without valid BSV authentication') &&
@@ -200,23 +201,35 @@ export class AuthFetch {
                         delete this.peers[baseURL];
                         config.retryCounter ??= 3;
                         const response = await this.fetch(url, config);
+                        handledRetry = true;
                         resolve(response);
                         return;
                     }
                     if (error.message.includes('HTTP server failed to authenticate')) {
                         try {
                             const response = await this.handleFetchAndValidate(url, config, peerToUse);
+                            handledRetry = true;
                             resolve(response);
                             return;
                         }
                         catch (fetchError) {
+                            handledRetry = true;
                             reject(fetchError);
                         }
                     }
                     else {
+                        handledRetry = true;
                         reject(error);
                     }
                 });
+                if (!handledRetry && this.callbacks[requestNonceAsBase64] != null) {
+                    peerToUse.peer.stopListeningForGeneralMessages(listenerId);
+                    delete this.callbacks[requestNonceAsBase64];
+                    delete this.peers[baseURL];
+                    config.retryCounter ??= 3;
+                    const response = await this.fetch(url, config);
+                    resolve(response);
+                }
             }
             catch (e) {
                 reject(e);
diff --git a/node_modules/@bsv/sdk/dist/esm/src/auth/transports/SimplifiedFetchTransport.js b/node_modules/@bsv/sdk/dist/esm/src/auth/transports/SimplifiedFetchTransport.js
index 314b7d0..d0603ef 100644
--- a/node_modules/@bsv/sdk/dist/esm/src/auth/transports/SimplifiedFetchTransport.js
+++ b/node_modules/@bsv/sdk/dist/esm/src/auth/transports/SimplifiedFetchTransport.js
@@ -68,7 +68,7 @@ export class SimplifiedFetchTransport {
                         }
                         if (this.onDataCallback != null) {
                             const responseMessage = await response.json();
-                            this.onDataCallback(responseMessage);
+                            await this.onDataCallback(responseMessage);
                         }
                         if (message.messageType === 'initialRequest') {
                             resolve();
@@ -206,7 +206,7 @@ export class SimplifiedFetchTransport {
                 throw this.createUnauthenticatedResponseError(url, response, responseBodyArray);
             }
             // Handle the response if data is received and callback is set
-            this.onDataCallback(responseMessage);
+            await this.onDataCallback(responseMessage);
         }
     }
     /**
@@ -217,11 +217,8 @@ export class SimplifiedFetchTransport {
      * @returns A promise that resolves once the callback is set.
      */
     async onData(callback) {
-        this.onDataCallback = (m) => {
-            void callback(m).catch(() => {
-                // Errors from handleIncomingMessage on the client side are not
-                // actionable here — prevent unhandled promise rejections.
-            });
+        this.onDataCallback = async (m) => {
+            await callback(m);
         };
     }
     createNetworkError(url, originalError) {
diff --git a/node_modules/@bsv/sdk/src/auth/Peer.ts b/node_modules/@bsv/sdk/src/auth/Peer.ts
index c2499ab..9be1b3b 100644
--- a/node_modules/@bsv/sdk/src/auth/Peer.ts
+++ b/node_modules/@bsv/sdk/src/auth/Peer.ts
@@ -394,8 +394,9 @@ export class Peer {
       requestedCertificates: this.certificatesToRequest
     }
 
+    const initialResponse = this.waitForInitialResponse(sessionNonce)
     await this.transport.send(initialRequest)
-    return await this.waitForInitialResponse(sessionNonce)
+    return await initialResponse
   }
 
   /**
@@ -889,17 +890,18 @@ export class Peer {
     }
 
     const peerSession = await this.sessionManager.getSession(message.yourNonce as string)
-    if (peerSession == null) {
+    const recoveredStatelessSession = peerSession == null
+    if (peerSession == null && typeof message.identityKey !== 'string') {
       throw new Error(`Session not found for nonce: ${message.yourNonce as string}`)
     }
 
-    const certificatesRequired = peerSession.certificatesRequired === true
-    const certificatesValidated = peerSession.certificatesValidated === true
+    const certificatesRequired = peerSession?.certificatesRequired === true
+    const certificatesValidated = peerSession?.certificatesValidated === true
 
     // If certificates are required but not yet validated, wait for them with a timeout
     if (certificatesRequired && !certificatesValidated) {
       const CERTIFICATE_WAIT_TIMEOUT_MS = 30000
-      const sessionNonce = peerSession.sessionNonce
+      const sessionNonce = peerSession?.sessionNonce
 
       if (sessionNonce == null) {
         throw new Error('Session nonce is required for certificate validation')
@@ -912,7 +914,7 @@ export class Peer {
           if (promise != null) {
             this.certificateValidationPromises.delete(sessionNonce)
             reject(new Error(
-              `Timeout waiting for certificate validation from peer ${peerSession.peerIdentityKey ?? 'unknown'
+              `Timeout waiting for certificate validation from peer ${peerSession?.peerIdentityKey ?? 'unknown'
               }`
             ))
           }
@@ -936,23 +938,28 @@ export class Peer {
       })
     }
 
+    const sessionNonceForSignature = peerSession?.sessionNonce ?? message.yourNonce as string
+    const counterpartyForSignature = peerSession?.peerIdentityKey ?? message.identityKey
+
     const { valid } = await this.wallet.verifySignature({
       data: message.payload,
       signature: message.signature as number[],
       protocolID: [2, 'auth message signature'],
-      keyID: `${message.nonce ?? ''} ${peerSession.sessionNonce ?? ''}`,
-      counterparty: peerSession.peerIdentityKey
+      keyID: `${message.nonce ?? ''} ${sessionNonceForSignature}`,
+      counterparty: counterpartyForSignature
     }, this.originator)
 
     if (!valid) {
       throw new Error(
-        `Invalid signature in generalMessage from ${peerSession.peerIdentityKey as string}`
+        `Invalid signature in generalMessage from ${counterpartyForSignature as string}`
       )
     }
 
-    // Mark last usage
-    peerSession.lastUpdate = Date.now()
-    await this.sessionManager.updateSession(peerSession)
+    if (!recoveredStatelessSession && peerSession != null) {
+      // Mark last usage
+      peerSession.lastUpdate = Date.now()
+      await this.sessionManager.updateSession(peerSession)
+    }
 
     // Update lastInteractedWithPeer
     this.lastInteractedWithPeer = message.identityKey
diff --git a/node_modules/@bsv/sdk/src/auth/clients/AuthFetch.ts b/node_modules/@bsv/sdk/src/auth/clients/AuthFetch.ts
index 93612fc..76f351e 100644
--- a/node_modules/@bsv/sdk/src/auth/clients/AuthFetch.ts
+++ b/node_modules/@bsv/sdk/src/auth/clients/AuthFetch.ts
@@ -269,6 +269,7 @@ export class AuthFetch {
         }
 
         // Send the request, now that all listeners are set up
+        let handledRetry = false
         await peerToUse.peer.toPeer(writer.toArray(), peerToUse.identityKey).catch(async error => {
           const isStaleSession =
             error.message.includes('Session not found for nonce') ||
@@ -282,21 +283,33 @@ export class AuthFetch {
             delete this.peers[baseURL]
             config.retryCounter ??= 3
             const response = await this.fetch(url, config)
+            handledRetry = true
             resolve(response)
             return
           }
           if (error.message.includes('HTTP server failed to authenticate')) {
             try {
               const response = await this.handleFetchAndValidate(url, config, peerToUse)
+              handledRetry = true
               resolve(response)
               return
             } catch (fetchError) {
+              handledRetry = true
               reject(fetchError)
             }
           } else {
+            handledRetry = true
             reject(error)
           }
         })
+        if (!handledRetry && this.callbacks[requestNonceAsBase64] != null) {
+          peerToUse.peer.stopListeningForGeneralMessages(listenerId)
+          delete this.callbacks[requestNonceAsBase64]
+          delete this.peers[baseURL]
+          config.retryCounter ??= 3
+          const response = await this.fetch(url, config)
+          resolve(response)
+        }
       } catch (e) {
         reject(e)
       }
diff --git a/node_modules/@bsv/sdk/src/auth/transports/SimplifiedFetchTransport.ts b/node_modules/@bsv/sdk/src/auth/transports/SimplifiedFetchTransport.ts
index 40b2f42..3648f18 100644
--- a/node_modules/@bsv/sdk/src/auth/transports/SimplifiedFetchTransport.ts
+++ b/node_modules/@bsv/sdk/src/auth/transports/SimplifiedFetchTransport.ts
@@ -13,7 +13,7 @@ const defaultFetch: typeof fetch =
  * This class integrates with fetch to send and receive authenticated messages between peers.
  */
 export class SimplifiedFetchTransport implements Transport {
-  private onDataCallback?: (message: AuthMessage) => void
+  private onDataCallback?: (message: AuthMessage) => Promise<void>
   fetchClient: typeof fetch
   baseUrl: string
 
@@ -80,7 +80,7 @@ export class SimplifiedFetchTransport implements Transport {
 
             if (this.onDataCallback != null) {
               const responseMessage = await response.json()
-              this.onDataCallback(responseMessage as AuthMessage)
+              await this.onDataCallback(responseMessage as AuthMessage)
             }
 
             if (message.messageType === 'initialRequest') {
@@ -229,7 +229,7 @@ export class SimplifiedFetchTransport implements Transport {
       }
 
       // Handle the response if data is received and callback is set
-      this.onDataCallback(responseMessage)
+      await this.onDataCallback(responseMessage)
     }
   }
 
@@ -241,11 +241,8 @@ export class SimplifiedFetchTransport implements Transport {
    * @returns A promise that resolves once the callback is set.
    */
   async onData(callback: (message: AuthMessage) => Promise<void>): Promise<void> {
-    this.onDataCallback = (m) => {
-      void callback(m).catch(() => {
-        // Errors from handleIncomingMessage on the client side are not
-        // actionable here — prevent unhandled promise rejections.
-      })
+    this.onDataCallback = async (m) => {
+      await callback(m)
     }
   }
 
